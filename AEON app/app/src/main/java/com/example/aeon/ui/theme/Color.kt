package com.example.aeon.ui.theme

import androidx.compose.ui.graphics.Color

// One accent: black. State is carried by fill, weight and rule, never by hue --
// the same restraint as the PC dashboard, so the two screens read as one system.
val Ink = Color(0xFF000000)
val Ink2 = Color(0xFF4A4A4A)
val Ink3 = Color(0xFF8A8A8A)
val Ink4 = Color(0xFFC2C2C2)
val Ground = Color(0xFFFFFFFF)
val Rule = Color(0xFFE9E9E9)
val Rule2 = Color(0xFFD2D2D2)
val Wash = Color(0xFFF4F4F4)
